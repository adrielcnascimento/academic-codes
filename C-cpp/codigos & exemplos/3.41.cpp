/*MULTIPLO DE 2 */
#include <stdio.h>
#include <stdlib.h>

int main ( ) {
    int cont = 0;
    while (cont >= 0){
          cont ++;
          if ( cont % 2 == 0)
          printf ("%d \n", cont);
}          
system ("pause");
return 0;
}
